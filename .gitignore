# Dependencies
node_modules/

# Database & backups
server-data.json
server-data.backup.json
server-data-backups/
System backups/

# Runtime-generated security/integrity state
integrity.json
sessions.json

# Environment
.env
.env.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Certs (if not committed)
certs/*.key
certs/*.crt

# Build artifacts
dist/
build/
*.exe

# Pre-compressed gzip (regenerated at runtime if needed)
*.gz

seed-books-api.js

seed-books-api.js

# JaaS keys (never commit)
jaasauth.key
jaasauth.key.pub
gen-jaas-key.js

# Deploy credentials & live access (never commit or upload)
DEPLOY_ENV.md

# Runtime doc store (PDFs/templates/generated letters) - managed on the live volume
docs/

# Local dev artifacts
mirror-desktop.ps1
test-diploma-local.js
test-diploma-output.pdf

# Live backup snapshots & ad-hoc access creds (never commit/upload)
live-pw.txt
System backups/

# Oracle VM SSH keys for migration (never commit/upload)
deploy-ssh/

# Word/design assets & exports at repo root (data, not code)
/*.pdf
/*.png
